apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-demo-ui
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: argocd-demo-ui
  template:
    metadata:
      labels:
        app: argocd-demo-ui
    spec:
      containers:
      - image: registry.k8s.io/e2e-test-images/agnhost:2.39
        name: hello-node-ui
        ports:
        - containerPort: 8080