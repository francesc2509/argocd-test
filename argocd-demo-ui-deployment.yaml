apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-demo-ui
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: argocd-demo-ui
  template:
    metadata:
      labels:
        app: argocd-demo-ui
    spec:
      containers:
      - image: gcr.io/heptio-images/ks-guestbook-demo:0.2
        name: guestbook-ui
        ports:
        - containerPort: 80